#!/usr/bin/env bash
set -euo pipefail

source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/foundrygate-service-lib.sh"

case "$(foundrygate_platform)" in
  Darwin)
    foundrygate_launchctl_start
    ;;
  *)
    sudo systemctl start foundrygate.service
    ;;
esac
